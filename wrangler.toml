# Cloudflare Pages 配置
name = "cloudflare-image-host"
compatibility_date = "2024-01-15"

# Pages Functions 配置
pages_build_output_dir = "dist"

# 注意：对于 Pages Functions，KV、R2 和环境变量
# 应该在 Cloudflare Dashboard 的 Pages 项目设置中配置
# 而不是在 wrangler.toml 中
